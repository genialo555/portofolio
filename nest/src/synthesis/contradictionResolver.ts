import { MergedInsights, ResolvedInsights } from '../types';

/**
 * Résolveur de contradictions
 */
export const contradictionResolver = {
  /**
   * Résout les contradictions potentielles
   * @param mergedInsights Insights fusionnés
   * @returns Insights résolus
   */
  resolve(mergedInsights: MergedInsights): ResolvedInsights {
    // Implémentation minimale pour résoudre l'erreur d'import
    return {} as ResolvedInsights;
  }
}; 