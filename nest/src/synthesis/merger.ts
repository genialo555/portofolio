import { DebateResult, MergedInsights } from '../types';

/**
 * Fusionneur des insights du débat
 */
export const merger = {
  /**
   * Fusionne les insights du débat
   * @param debateResult Résultat du débat
   * @returns Insights fusionnés
   */
  merge(debateResult: DebateResult): MergedInsights {
    // Implémentation minimale pour résoudre l'erreur d'import
    return {} as MergedInsights;
  }
}; 