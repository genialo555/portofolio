import { PoolOutputs } from '../types';

/**
 * Collecteur des outputs des pools d'agents
 */
export const outputCollector = {
  /**
   * Collecte et structure les outputs des pools
   * @param outputs Outputs bruts des pools
   * @returns Outputs structurés
   */
  collect(outputs: PoolOutputs): PoolOutputs {
    // Implémentation minimale pour résoudre l'erreur d'import
    return outputs;
  }
}; 